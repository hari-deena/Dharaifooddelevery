version: '3.9'

services:
  fastapi-app:
    build: .
    ports:
      - "8000:8000"
    environment:
      DB_USER=dharaifood
      DB_PASSWORD=DharaiFood123!
      DB_HOST=184.72.86.165
      DB_NAME=dharaifood
      PORT=3036
